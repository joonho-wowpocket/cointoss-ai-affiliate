import { SecureLeadCaptures } from '@/components/admin/SecureLeadCaptures';

export default function SecureLeadCapturesPage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <SecureLeadCaptures />
    </div>
  );
}